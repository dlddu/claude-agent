---
description: SDD(Spec Driven Development) 전체 워크플로우를 실행합니다. 계획 수립 → 구현 → CI 확인 → CI 수정의 사이클을 자동으로 진행합니다.
---

# Workflow Skill - SDD 전체 워크플로우 실행

이 skill은 Spec Driven Development의 전체 개발 사이클을 순차적으로 실행합니다.

## 워크플로우 개요

```
┌─────────────────────────────────────────────────────────────────┐
│                    SDD Development Workflow                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────┐    ┌────────────────┐    ┌──────────┐             │
│  │ Planning │───▶│ Implementation │───▶│ CI Wait  │             │
│  └──────────┘    └────────────────┘    └────┬─────┘             │
│       ▲                                     │                    │
│       │                                     ▼                    │
│       │                              ┌────────────┐              │
│       │         ┌────────────────────│  Success?  │              │
│       │         │                    └──────┬─────┘              │
│       │         │ Yes                       │ No                 │
│       │         ▼                           ▼                    │
│       │    ┌─────────┐              ┌──────────┐                │
│       │    │  Done   │              │ CI Fix   │                │
│       │    └─────────┘              └────┬─────┘                │
│       │                                  │                       │
│       │                                  ▼                       │
│       │                           ┌──────────┐                   │
│       └───────────────────────────│ CI Wait  │◀──────┐          │
│                                   └────┬─────┘       │          │
│                                        │             │          │
│                                        ▼             │          │
│                                  ┌──────────┐        │          │
│                                  │ Success? │────────┘          │
│                                  └──────────┘   No              │
│                                        │ Yes                     │
│                                        ▼                         │
│                                  ┌──────────────┐               │
│                                  │ Next Task?   │               │
│                                  └──────┬───────┘               │
│                                    Yes  │                        │
│                                         ▼                        │
│                                   Back to Planning               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 실행 단계

### Phase 1: Planning (계획 수립)

1. **현재 상태 확인**
   ```bash
   # 추적성 매트릭스 확인
   cat /specs/TRACEABILITY.md
   ```

2. **다음 작업 식별**
   - 상태가 `⏳ 대기`인 항목 중 우선순위가 높은 것 선택
   - 의존성이 해결된 작업만 선택

3. **명세서 작성/확인**
   - 명세서가 없으면 새로 작성
   - 기존 명세서가 있으면 검토 후 진행

4. **작업 시작 알림**
   ```
   다음 작업을 진행합니다:
   - Spec ID: FEAT-XXX
   - 제목: [작업 제목]
   - 예상 범위: [구현 범위]

   [자동 진행]
   ```

### Phase 2: Implementation (구현)

1. **추적성 매트릭스 업데이트**
   - 상태를 `🚧 진행중`으로 변경

2. **코드 구현**
   - 명세서의 요구사항에 따라 구현
   - `@spec` 주석으로 명세 참조

3. **테스트 작성**
   - 검증 기준에 맞는 테스트 코드 작성

4. **로컬 검증**
   ```bash
   pnpm lint && pnpm typecheck && pnpm test && pnpm build
   ```

5. **커밋**
   ```bash
   git add .
   git commit -m "feat: [구현 내용] - [Spec ID]"
   ```

### Phase 3: CI Wait (CI 대기)

1. **Push**
   ```bash
   git push -u origin <branch-name>
   ```

2. **CI 모니터링**
   ```bash
   gh run list --branch <branch-name> --limit 1
   gh run watch <run-id> --exit-status
   ```

3. **결과 확인**
   - 성공: Phase 4로 진행
   - 실패: CI Fix 단계로 이동

### Phase 4: CI Fix (CI 수정) - 필요시

1. **실패 로그 분석**
   ```bash
   gh run view <run-id> --log-failed
   ```

2. **오류 수정**
   - 로컬에서 동일한 오류 재현
   - 수정 후 로컬 검증

3. **재커밋 및 Push**
   ```bash
   git add .
   git commit -m "fix: [수정 내용] - [Spec ID]"
   git push
   ```

4. **CI 재확인**
   - 성공할 때까지 반복 (최대 5회)

### Phase 5: Completion (완료)

1. **추적성 매트릭스 업데이트**
   - 상태를 `✅ 완료`로 변경
   - 구현 파일, 테스트 파일 경로 기록

2. **명세서 업데이트**
   - 요구사항 체크리스트 완료 처리
   - 변경 이력 기록

3. **다음 작업 자동 진행**
   ```
   현재 작업이 완료되었습니다.

   ## 완료된 작업
   - Spec ID: FEAT-XXX
   - 상태: ✅ 완료

   ## 남은 작업
   - FEAT-YYY: [제목] (⏳ 대기)
   - API-ZZZ: [제목] (⏳ 대기)

   [다음 작업 자동 진행]
   ```

## 워크플로우 옵션

### 전체 자동 실행 (기본값)
```
workflow 실행
```
- 모든 대기 중인 작업을 순차적으로 처리
- 사용자 확인 없이 자동 진행

### 단일 작업 실행
```
workflow 실행 --single
```
- 하나의 작업만 완료 후 종료

### 특정 명세 실행
```
workflow 실행 --spec FEAT-001
```
- 지정된 명세만 구현

### CI 수정 건너뛰기
```
workflow 실행 --skip-ci-fix
```
- CI 실패 시 수정 없이 중단하고 보고

## 상태 보고 형식

```
═══════════════════════════════════════════════════════════════
                    SDD Workflow Progress
═══════════════════════════════════════════════════════════════

Current Task: FEAT-001 - System Architecture
Phase: Implementation [████████░░] 80%

───────────────────────────────────────────────────────────────
Phase Status:
  [✓] Planning      - Completed
  [►] Implementation - In Progress (3/4 requirements done)
  [ ] CI Wait       - Pending
  [ ] CI Fix        - Pending (if needed)
  [ ] Completion    - Pending
───────────────────────────────────────────────────────────────

Session Summary:
  Tasks Completed: 2
  Tasks Remaining: 9
  CI Runs: 3 (2 success, 1 fixed)

═══════════════════════════════════════════════════════════════
```

## 중단 및 재개

### 워크플로우 중단
- `Ctrl+C` 또는 사용자 요청으로 중단
- 현재 상태가 저장됨

### 워크플로우 재개
```
workflow 재개
```
- 마지막 중단 지점부터 계속 진행

## 주의사항

1. **자동 진행**: 모든 단계가 사용자 확인 없이 자동으로 진행됩니다
2. **롤백 불가**: 커밋된 변경사항은 자동으로 롤백되지 않습니다
3. **CI 수정 제한**: 5회 이상 CI 실패 시 자동 중단하고 상태를 보고합니다
4. **명세 준수**: 명세서에 없는 기능은 구현하지 않습니다
5. **순차 실행**: 의존성이 있는 작업은 순서대로 실행됩니다

## 예시 실행

```
> workflow 실행

═══════════════════════════════════════════════════════════════
Starting SDD Workflow (Auto Mode)
═══════════════════════════════════════════════════════════════

[Phase 1: Planning]
Analyzing TRACEABILITY.md...
Found 11 pending tasks.

Next task identified:
- Spec ID: FEAT-001
- Title: System Architecture
- Priority: High
- Dependencies: None

[자동 진행]

[Phase 2: Implementation]
Updating status to 🚧 진행중...
Implementing requirements...
  [✓] REQ-1: 모노레포 구조 설정
  [✓] REQ-2: 패키지 구조 정의
  [✓] REQ-3: 공통 설정 파일 생성
  [✓] REQ-4: 개발 환경 스크립트

[Phase 3: CI Wait]
Pushing to origin/feature/feat-001...
Monitoring GitHub Actions...
Run #12345: ✅ All jobs passed (5m 30s)

[Phase 5: Completion]
Updating TRACEABILITY.md...
Task FEAT-001 completed successfully!

[다음 작업 자동 진행]

[Returning to Phase 1...]
```
